#!/bin/sh

WINDOW=$(xdotool search --onlyvisible --maxdepth 1 --class Firefox)

if [ -z "$WINDOW" ]
then
  firefox --new-window "$@"
else
  xdotool windowactivate $WINDOW
  firefox --new-tab "$@"
fi

